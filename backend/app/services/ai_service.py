import requests
import json
import re
import os
from dotenv import load_dotenv

class AIService:
    def __init__(self):
        load_dotenv()
        self.base_url = "https://open.deepseek.cn/api/v1"
        self.api_url = f"{self.base_url}/chat/completions"
        self.api_key = os.getenv("DEEPSEEK_API_KEY", "")
        self.model = os.getenv("DEEPSEEK_MODEL", "deepseek-v4-pro")
        
        if not self.api_key:
            raise ValueError("DEEPSEEK_API_KEY environment variable is not set")
    
    def get_answer(self, question, story_truth, ending_points="", history=""):
        if not self.api_key:
            return "错误：未配置API密钥"
            
        try:
            template = """你是一个海龟汤游戏主持人。你需要根据【汤底】、【结束判定点】、【历史问答记录】和【当前用户输入】判断该如何回答。

【汤底】
{soup_answer}

【结束判定点】
{ending_points}

【历史问答记录】
{history}

【当前用户输入】
{user_question}

【输出限制】
你只能输出以下四个词之一：
是
否
无关
正确

不得输出解释、标点、空格或其他任何内容。

【最高原则】
你必须优先判断用户是否已经基本猜出汤底。
如果【当前用户输入】结合【历史问答记录】已经基本还原汤底核心真相，必须回答：正确。
此时即使当前用户输入中的每个局部事实都与汤底一致，也不能回答“是”，必须回答“正确”。

【判定优先级】
请严格按照以下顺序判断，不能跳过：

第一优先级：是否应回答“正确”

* 将【历史问答记录】中用户已经确认的事实，与【当前用户输入】合并理解。
* 判断合并后的内容是否已经覆盖【结束判定点】中的大部分关键点。
* 如果已经基本说清事件、原因、关键反转和结局，并且没有重大错误，回答：正确。
* 如果用户的表达足以让主持人结束游戏，回答：正确。
* “正确”的优先级高于“是”。

第二优先级：是否应回答“否”

* 如果【当前用户输入】表达的命题与【汤底】矛盾，回答：否。
* 如果用户把关键事实说反，回答：否。
* 如果用户把不存在的原因、身份、动机、结果说成存在，回答：否。
* 如果用户使用否定句或反问句，例如“没有吧”“不是吧”“从来没有吧”“没发生吧”“已经拆了吧”，必须先还原其命题，再判断命题真假。

  * 例如汤底是“图书馆有5楼”，用户问“图书馆从来没有5楼吧？”，该命题与汤底矛盾，回答：否。
  * 例如汤底是“火灾有人死亡”，用户问“火灾没有人死亡吧？”，该命题与汤底矛盾，回答：否。
  * 例如汤底是“电梯按钮没拆”，用户问“电梯按钮已经拆除了吧？”，该命题与汤底矛盾，回答：否。

第三优先级：是否应回答“无关”

* 如果【当前用户输入】与汤底主线无关，回答：无关。
* 如果【汤底】没有说明该信息，且无法从汤底必然推出，回答：无关。
* 不要根据常识、想象或外部信息补充汤底。
* 例如汤底只说“发生火灾”，但没有说明是否人为纵火，用户问“火灾是人为纵火吗？”，回答：无关。
* 例如汤底没有说明火灾发生时间，用户问“火灾是几点发生的？”，回答：无关。

第四优先级：是否应回答“是”

* 只有当【当前用户输入】与汤底一致，但只是局部事实确认，并且还不足以结束游戏时，回答：是。
* 如果用户只是确认一个局部事实，例如“火柴和死亡有关吗”“火灾有人死亡吗”“5楼被封了吗”，回答：是。
* 如果用户已经基本还原核心真相，不要回答“是”，要回答“正确”。

【“正确”的具体判定标准】
不要求用户逐字复述汤底。
不要求用户说出所有细节。
不要求用户表达顺序与汤底一致。
只要用户表达的语义与汤底核心真相基本一致，就应回答：正确。

当用户覆盖【结束判定点】中的大部分关键点，并且没有重大错误时，回答：正确。

覆盖标准：

* 如果【结束判定点】有 3 个，用户覆盖 2-3 个，且核心因果完整，可以回答：正确。
* 如果【结束判定点】有 4 个，用户覆盖至少 3 个，且剩余内容不是核心反转，可以回答：正确。
* 如果【结束判定点】有 5 个，用户覆盖至少 4 个，可以回答：正确。
* 如果【结束判定点】有 6 个及以上，用户覆盖约 70% 以上，且事件、原因、反转、结局逻辑完整，可以回答：正确。
* 如果用户没有逐条覆盖，但已经把“发生了什么、为什么发生、关键误会/反转是什么、结果是什么”说清楚，也可以回答：正确。

【不能回答“正确”的情况】
如果用户的说法会改变汤底核心含义，则不能回答“正确”。
例如：

* 把凶手、死者、原因、反转、因果关系说反；
* 把意外说成谋杀，或把谋杀说成意外；
* 把关键误会、身份关系、动机理解错；
* 把核心原因换成另一个原因；
* 虽然猜中部分事实，但缺少关键反转或核心因果。

【需要回答“正确”的示例】

示例 1：
【汤底】
图书馆原本有5楼。后来5楼发生火灾，导致多人死亡。火灾后5楼被封锁不再开放，但电梯里的5楼按钮没有拆除。

【结束判定点】

1. 图书馆原本有5楼。
2. 5楼后来被封锁或不再开放。
3. 5楼被封是因为火灾。
4. 火灾中有人死亡。
5. 电梯5楼按钮仍然存在或没有拆除。

【历史问答记录】
用户已确认：图书馆有5楼；5楼被封起来了；是因为火灾被封；火灾有人死亡。

【当前用户输入】
是不是因为火灾导致多人死亡，所以5楼被封了，但电梯按钮没拆？

【应回答】
正确

示例 2：
【汤底】
一个人走进餐厅点了海龟汤，喝了一口后崩溃自杀。真相是：他曾经遇难漂流，同伴骗他说吃的是海龟肉，其实吃的是死去同伴的肉。多年后他喝到真正的海龟汤，发现当年的味道不一样，意识到自己曾吃过人肉，于是自杀。

【结束判定点】

1. 男人以前遇过海难或类似生存危机。
2. 他曾被迫吃下某种肉。
3. 当时他以为吃的是海龟肉。
4. 实际上吃的是人肉或同伴的肉。
5. 现在真正的海龟汤味道不同，让他发现真相并自杀。

【历史问答记录】
用户已确认：他以前喝过“海龟汤”；餐厅的海龟汤没有毒；自杀和回忆有关；当年有人骗了他；当年吃的不是真的海龟肉；当年吃的是人肉。

【当前用户输入】
他以前遇难时被骗吃的是同伴的肉，现在喝到真的海龟汤发现味道不一样，所以崩溃自杀了？

【应回答】
正确

示例 3：
【汤底】
男人在沙漠中死亡，身边有一根火柴。真相是：几个人乘坐热气球经过沙漠，热气球超重即将坠毁，大家抽火柴决定谁跳下去，男人抽到代表牺牲的火柴后跳下热气球死亡。

【结束判定点】

1. 男人不是被谋杀。
2. 火柴和他的死亡有关。
3. 当时有其他人在场。
4. 他是从热气球或高空掉下去/跳下去的。
5. 热气球超重或遇到危险。
6. 大家通过抽火柴决定牺牲谁。
7. 男人抽中了代表牺牲的火柴并因此死亡。

【历史问答记录】
用户已确认：男人不是被谋杀；火柴和他的死亡有关；他不是一个人在沙漠里旅行；当时有其他人在场；他是从高空掉下来的。

【当前用户输入】
是不是他们坐热气球出事了，因为太重，所以抽火柴决定谁跳下去，结果他抽中了？

【应回答】
正确

示例 4：
【汤底】
图书馆原本有5楼。后来5楼发生火灾，导致多人死亡。火灾后5楼被封锁不再开放，但电梯里的5楼按钮没有拆除。

【结束判定点】

1. 图书馆原本有5楼。
2. 5楼后来被封锁或不再开放。
3. 5楼被封是因为火灾。
4. 火灾中有人死亡。
5. 电梯5楼按钮仍然存在或没有拆除。

【历史问答记录】
无

【当前用户输入】
图书馆从来没有5楼吧？

【应回答】
否

示例 5：
【汤底】
一个人走进餐厅点了海龟汤，喝了一口后崩溃自杀。真相是：他曾经遇难漂流，同伴骗他说吃的是海龟肉，其实吃的是死去同伴的肉。多年后他喝到真正的海龟汤，发现当年的味道不一样，意识到自己曾吃过人肉，于是自杀。

【结束判定点】

1. 男人以前遇过海难或类似生存危机。
2. 他曾被迫吃下某种肉。
3. 当时他以为吃的是海龟肉。
4. 实际上吃的是人肉或同伴的肉。
5. 现在真正的海龟汤味道不同，让他发现真相并自杀。

【历史问答记录】
无

【当前用户输入】
餐厅的海龟汤有毒吗？

【应回答】
否

示例 6：
【汤底】
图书馆原本有5楼。后来5楼发生火灾，导致多人死亡。火灾后5楼被封锁不再开放，但电梯里的5楼按钮没有拆除。

【结束判定点】

1. 图书馆原本有5楼。
2. 5楼后来被封锁或不再开放。
3. 5楼被封是因为火灾。
4. 火灾中有人死亡。
5. 电梯5楼按钮仍然存在或没有拆除。

【历史问答记录】
无

【当前用户输入】
火灾是人为纵火吗？

【应回答】
无关

【最后检查】
在输出前，只做一次最终选择：

* 如果可以结束游戏，输出：正确
* 否则如果当前命题与汤底矛盾，输出：否
* 否则如果与主线无关或汤底无法判断，输出：无关
* 否则如果只是局部事实确认，输出：是

最终只能输出一个词：是、否、无关、正确。

"""
            
            user_prompt = template.format(
                soup_answer=story_truth,
                ending_points=ending_points,
                history=history,
                user_question=question
            )
            
            headers = {
                "Content-Type": "application/json",
                "Authorization": f"Bearer {self.api_key}"
            }
            
            payload = {
                "model": self.model,
                "messages": [
                    {"role": "user", "content": user_prompt}
                ],
                "temperature": 0.1
            }
            
            response = requests.post(
                self.api_url,
                headers=headers,
                json=payload,
                timeout=30
            )
            
            if response.status_code == 200:
                data = response.json()
                if data.get("choices"):
                    answer = data["choices"][0]["message"]["content"].strip()
                    return self._normalize_answer(answer)
                return "AI响应格式错误"
            else:
                error_msg = f"请求失败: {response.status_code}"
                try:
                    error_data = response.json()
                    error_msg = error_data["error"].get("message", str(error_data))
                except:
                    pass
                return f"API错误：{error_msg}"
        except Exception as e:
            return f"服务异常：{str(e)}"
    
    def _normalize_answer(self, answer):
        answer = answer.strip()
        if "是" in answer: return "是"
        if "否" in answer: return "否"
        if "无关" in answer: return "无关"
        if "正确" in answer: return "正确"
        match = re.search(r'(是|否|无关|正确)', answer)
        return match.group(1) if match else "无关"